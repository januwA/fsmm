import path from "path";
import { find, ft, line, cp, each, mv, rm } from "../src";

describe("main", () => {
  // it("test find", async () => {
  //   const paths = await find(["./*.json", "./*.md"]);
  //   expect(paths.length).not.toBe(0);
  //   expect(finds(["./*.json", "./*.md"]).length).not.toBe(0);
  // });
  // it("test ft", async () => {
  //   const fp = path.resolve(__dirname, "test.test.ts");
  //   expect(await ft("e", fp)).toBe(true);
  //   expect(await ft("f", fp)).toBe(true);
  //   expect(await ft("d", fp)).toBe(false);
  //   expect(await ft("T", fp)).toBe(true);
  //   expect(await ft("r", fp)).toBe(true);
  //   expect(await ft("w", fp)).toBe(true);
  //   expect(fts("e", fp)).toBe(true);
  //   expect(fts("f", fp)).toBe(true);
  //   expect(fts("d", fp)).toBe(false);
  //   expect(fts("T", fp)).toBe(true);
  //   expect(fts("r", fp)).toBe(true);
  //   expect(fts("w", fp)).toBe(true);
  // });
  // it("test open", async () => {
  //   const r = await line(path.resolve(__dirname, "test.test.ts"), (line) => {});
  //   expect(r.changeLine).toBe(0);
  // });

  it("test main", async () => {
  });
});
